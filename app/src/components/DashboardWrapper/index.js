import styled from 'styled-components';

const DasboardWrapper = styled.main`
  background-color: #fafafa;
  display: flex;
  flex-direction: column;
  height: 100vh;
`;

export default DasboardWrapper;
